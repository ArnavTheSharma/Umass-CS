import java.util.Set;
import java.util.HashSet;
import java.util.LinkedList;

// package quiz2;

public class Sets<E> {
    private Set<E> s;
    private LinkedList<E> l;

    static <E> Set<E> deduplicate(Set<E> list) {
        // we can use a set and if s.add(element) == true, then add it to the linked list. 
        
        Set<> s = new HashSet<>();
    }
}
