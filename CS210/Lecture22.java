public class Lecture22 {
    // Bloom filters
    //      Hashing is a way to store/access quickly, which we implemented using a fixed size array, and dealt with collisions with open addressing or chaining.
    //      Bloom filters store whether x is in a set or not, and so it only needs a 0 or 1. However we can sometimes have false positives (though the probability of this is low and tunable. It's so small we can ignore it)
    // Applications are things like checking if it's in a cache, or malicuous URLs (they have a database of this), or 



}
